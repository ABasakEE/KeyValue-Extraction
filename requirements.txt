# IE 643 Course Project — Cross-Template Key-Value Field Extraction
# Python 3.10+ (Kaggle notebooks ship 3.11; local dev verified on 3.12)

# --- core modelling ---
torch>=2.1
transformers>=4.40        # LiLT, BROS, LayoutLMv3 are all native from 4.30+
datasets>=2.18
accelerate>=0.28
seqeval>=1.2.2            # strict entity-level F1, the FUNSD/KIE convention
scikit-learn>=1.3

# --- data / documents ---
PyMuPDF>=1.23.0           # AcroForm widget filling for the synthetic template pipeline
Pillow>=10.0
Faker>=24.0               # synthetic field values
augraphy>=8.2             # scan/print degradation (Groleau et al., ICDAR 2023)

# --- config, reporting, plots ---
PyYAML>=6.0
matplotlib>=3.8
pandas>=2.0

# --- demo interface (Phase: post-prep) ---
gradio>=4.26              # gr.AnnotatedImage gives the upload -> highlighted-boxes UX

# --- optional, not needed for the pilot ---
# qwen-vl-utils          # only for the Qwen2.5-VL-7B-Instruct zero-shot baseline
# paddleocr              # only when we move off pre-provided OCR to our own scans
